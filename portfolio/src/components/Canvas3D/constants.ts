/** Canvas pixels → Three.js world units */
export const DEFAULT_WORLD_SCALE = 0.01;

/** Eye height for first-person camera (meters-style units) */
export const EYE_HEIGHT = 1.65;

/** Base movement speed (world units per second) */
export const DEFAULT_MOVE_SPEED = 16;

export const SPRINT_MULTIPLIER = 1.8;

/** Default perspective camera FOV (deg) */
export const DEFAULT_CAMERA_FOV = 70;
